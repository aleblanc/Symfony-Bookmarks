{
    "name": "sfbookmarks-sync",
    "version": "0.2.0",
    "description": "Firefox (Android) extension that syncs Symfony Bookmarks into browser bookmarks.",
    "private": true,
    "scripts": {
        "start": "web-ext run",
        "start:android": "web-ext run -t firefox-android",
        "lint": "web-ext lint --self-hosted",
        "build": "web-ext build --overwrite-dest --ignore-files node_modules package.json package-lock.json README.md",
        "sign": "web-ext sign --channel=unlisted --ignore-files node_modules package.json package-lock.json README.md"
    },
    "devDependencies": {
        "web-ext": "^10"
    }
}
