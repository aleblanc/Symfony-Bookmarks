{% macro icon(collection) %}
{%- set ic = collection.icon in ['', 'BM'] ? '📁' : collection.icon -%}
{%- set col = collection.color|default('#0ea5e9') -%}
<span class="folder-ic" style="--fic: {{ col }};">{{ ic }}</span>
{% endmacro %}
